# 의존성 / 빌드 산출물
node_modules/
dist/
coverage/

# npm pack 결과물
*.tgz

# 로그
*.log
npm-debug.log*

# OS
.DS_Store

# 별도 참조물 — 이후 워크아이템에서 조사한다. 여기서는 추적하지 않는다.
ref-knowledge-graph/
.awl/state.json
.awl/skills-version.json

# dogfooding 검증 산물 (WI-6 자기 검증용 격리 홈, 설치된 스킬)
.temp-loop-verify/
.claude/
.awl/verify-baseline.json

# 세션 계획/실행/검증 문서 (로컬 작업 문서 — git 미추적)
.tasks/
.awl-worktrees/
